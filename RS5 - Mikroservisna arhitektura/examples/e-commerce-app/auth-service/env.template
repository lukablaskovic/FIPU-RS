CLIENT_ID=YOUR_CLIENT_ID
CLIENT_SECRET=YOUR_CLIENT_SECRET
AUDIENCE=https://dev-example.auth0.com/api/v2
AUTH0_DOMAIN=dev-example.auth0.com

SERVER_HOST=localhost
SERVER_PORT=8000

# Optional (recommended): Auth0 Management API credentials for resolving the real
# "connection" name (e.g. "username-password-authentication").
# Create an Auth0 Machine-to-Machine app with `read:users` granted for the
# "Auth0 Management API" and fill these.
AUTH0_MGMT_CLIENT_ID=
AUTH0_MGMT_CLIENT_SECRET=
AUTH0_MGMT_AUDIENCE=https://dev-example.auth0.com/api/v2/
AUTH0_MGMT_SCOPE=read:users